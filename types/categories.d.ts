export interface Category {
    id: string; // Unique identifier for the category
    name: string; // Name of the category (e.g., "3D Designs")
    description: string; // A brief description of the category
    imageUrl: string; // URL for an image associated with the category
    link: string; // Link to the category page (e.g., "/products/3d-designs")
  }
  